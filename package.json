{
  "name": "alexandria",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:integration": "vitest run tests/",
    "deploy:local": "pnpm --filter @alexandrian/protocol deploy:local",
    "deploy:docker": "docker compose -f docker/docker-compose.yml run --rm -e CHAIN_RPC_URL=http://blockchain:8545 blockchain npx hardhat run scripts/deploy.cjs --network docker",
    "deploy:testnet": "pnpm --filter @alexandrian/protocol deploy:testnet",
    "start": "pnpm --filter @alexandrian/api start",
    "dev": "pnpm --filter @alexandrian/api dev",
    "demo": "pnpm exec ts-node scripts/demo.ts",
    "demo:full": "node scripts/run-full-demo.mjs",
    "demo:slash": "pnpm exec ts-node scripts/demo.ts --slash",
    "demo:dispute": "pnpm exec ts-node scripts/demo.ts --dispute",
    "test:pipeline": "node scripts/test-pipeline.mjs",
    "test:spec": "vitest run tests/unit/",
    "generate:test-vectors": "node scripts/generate-test-vector-expected.mjs",
    "subgraph:codegen": "pnpm --filter alexandrian-protocol-subgraph codegen",
    "subgraph:build": "pnpm --filter alexandrian-protocol-subgraph build"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.8.0",
    "node-fetch": "^3.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0",
    "supertest": "^6.3.0",
    "express": "^4.21.0",
    "@types/express": "^4.17.21"
  },
  "pnpm": {
    "overrides": {
      "redis": "5.10.0",
      "parse-duration": "2.1.3",
      "expr-eval": "2.0.2",
      "hardhat": "2.28.6",
      "@nomicfoundation/hardhat-ethers": "3.1.3",
      "@nomicfoundation/hardhat-verify": "2.0.11",
      "@nomicfoundation/hardhat-network-helpers": "1.0.11"
    }
  }
}