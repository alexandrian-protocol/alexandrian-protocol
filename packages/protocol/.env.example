# Alexandrian Protocol — Environment Variables
# Copy to .env and set values. Never commit .env.

# Deployer key (required for testnet deploy and CLI). Use a testnet-only key.
PRIVATE_KEY=

# RPC URLs (optional; defaults below)
# BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# SEPOLIA_RPC_URL=https://rpc.sepolia.org
# CHAIN_RPC_URL=http://127.0.0.1:8545

# Optional gas price (wei) to reduce cost on testnets
# BASE_SEPOLIA_GAS_PRICE=1000000000
# SEPOLIA_GAS_PRICE=1000000000

# After deploy: set KnowledgeRegistry address (from deploy output or deployments/KnowledgeRegistry.json)
# KNOWLEDGE_REGISTRY_ADDRESS=

# Subgraph: query URL (for integration tests) and deploy (from The Graph Studio)
# SUBGRAPH_QUERY_URL=https://api.studio.thegraph.com/query/.../.../version/latest
# GRAPH_STUDIO_DEPLOY_KEY=
# SUBGRAPH_SLUG=alexandria

# Optional: gas reporter
# REPORT_GAS=true
