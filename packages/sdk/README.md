# @alexandrian/sdk

Agent-facing SDK for the Alexandrian Protocol: `publish`, `query`, `settleCitation`, `getReputation`. Exposes `asContext()` (prompt injection) and `asStructured()` (programmatic execution) consumption modes.
